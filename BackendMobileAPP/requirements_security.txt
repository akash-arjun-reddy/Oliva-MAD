# Security and Authentication Dependencies
# Run: pip install -r requirements_security.txt

# MFA Support
pyotp==2.9.0
qrcode[pil]==7.4.2

# Enhanced JWT handling
python-jose[cryptography]==3.3.0

# Rate limiting and security
slowapi==0.1.9

# Additional security utilities
cryptography==41.0.7
passlib[bcrypt]==1.7.4

# Database and ORM (if not already installed)
sqlalchemy==2.0.23
alembic==1.12.1

# HTTP and API
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Google OAuth (if not already installed)
google-auth==2.23.4
google-auth-oauthlib==1.1.0

# Utilities
python-multipart==0.0.6
pydantic==2.5.0
pydantic-settings==2.1.0

# Development and testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2
