import 'package:flutter/material.dart';

class SkinConcernPage extends StatelessWidget {
  const SkinConcernPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Skin Concern')),
      body: Center(child: Text('Skin Concern Page')),
    );
  }
}
